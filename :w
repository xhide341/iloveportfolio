import UserCard from "~/components/contributors/card/card";

export default function ContributorsPage() {
  type Contributors = {
    imageUrl: string;
    link: string;
    username: string;
  };

  const backend: Contributors[] = [
    {
      link: "https://github.com/xhide341",
      imageUrl: "/contributors/hide.jpeg",
      username: "hide",
    },
  ];

  const frontend: Contributors[] = [
    {
      link: "https://github.com/justinecua",
      imageUrl: "/contributors/cua.jpeg",
      username: "Justine Cua",
    },
    {
      link: "https://github.com/Yncy0",
      imageUrl: "/contributors/yncy0.png",
      username: "Yncy0",
    },
    {
      link: "https://github.com/xhide341",
      imageUrl: "/contributors/hide.jpeg",
      username: "hide",
    },
  ];

  return (
    <main className="flex h-full w-[1200px] flex-col items-start">
      <h1 className="text-4xl font-bold">Contributors</h1>

      <section className="mt-28">
        <section>
          <h2 className="text-2xl font-bold">Back-end</h2>
          <p>Contributors that develop back-end systems such as A.I. systems and Authentication systems</p>
        </section>
        <section className="flex w-[1200px] flex-row flex-wrap gap-5">
          {backend.map((item, index) => (
            <UserCard
              key={index}
              imageUrl={item.imageUrl}
              link={item.link}
              username={item.username}
            />
          ))}
        </section>
      </section>

      <section className="mt-16">
        <section>
          <h2 className="text-2xl font-bold">Front-end</h2>
          <p>Contributors that develop aninteractive GUI for the users</p>
        </section>
        <section className="flex w-[1200px] flex-row flex-wrap gap-5">
          {frontend.map((item, index) => (
            <UserCard
              key={index}
              imageUrl={item.imageUrl}
              link={item.link}
              username={item.username}
            />
          ))}
        </section>
      </section>
    </main>
  );
}
